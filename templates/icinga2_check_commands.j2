# {{ ansible_managed }}

{% for key,value in icinga2_check_commands.iteritems() %}
object CheckCommand "{{ key }}" {
  import "plugin-check-command"

  command = [
    PluginDir + "/{{ key }}",
    {{ value }}
  ]
}

{% endfor %}
